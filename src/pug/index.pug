extends layouts/default.pug

block content
  main
    section.menu
      .menu__select-zone
        h2 Instruction Options
        p.instructions Choose which topics will be taught in the library session by dragging and dropping the modules from the left to the right columns. Change the duration for each topic according to your needs. The total duration of the library session will be shown at the bottom.
        .drop-zone-component__length-minutes
          input(type="text" value="0").length-minutes-input#time-input
          p How long is your class in minutes?
        ul#instruction-cards.card-connect
          //- li.instruction-card
          //-   .instruction-card__handle
          //-     .dot-row
          //-       .dot
          //-       .dot
          //-     .dot-row
          //-       .dot
          //-       .dot
          //-     .dot-row
          //-       .dot
          //-       .dot
          //-   .instruction-card__content
          //-     .instruction-card__title
          //-       h4 Background Research
          //-     .instruction-card__description
          //-       p description
          //-     .instruction-card__tools
          //-       .instruction-card__modal-link
          //-         a(href="#") More Info
          //-       .instruction-card__time-default
          //-         input(type="text" value="0").time-default-input

      .menu__drop-zone.drop-zone
        header
          .drop-zone-component__title
            h2 Build Your Class
        section
          ul#instruction-cards-drop-zone.card-connect.bgImage
        footer
          .drop-zone-component__class-time
            p Class time
            input(type="text" value="0 / 0" readonly).class-time-input#time-counter
          .drop-zone-component__next-button
            button#menu-step-one Next Step